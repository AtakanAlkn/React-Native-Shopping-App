import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    height: 40,
    marginTop: 10,
    padding: 10,
    backgroundColor: '#ECEFF1',
    borderRadius: 10,
    borderWidth: 0,
    color: 'gray',
  },
});
